name: Auto Post Reviews Daily

on:
  schedule:
    # 毎日AM4時（JST）に実行（UTC 19:00）
    - cron: '0 19 * * *'
  workflow_dispatch: # 手動実行も可能

jobs:
  post-reviews:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run daily reviews script (v4 - 5 types support)
        env:
          AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
          AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID }}
          REVIEW_ROUNDS: 1  # 自動実行では1ラウンドのみ（手動実行時は環境変数で変更可能）
        run: node scripts/run-daily-reviews-v4.cjs

      - name: Verify execution and data integrity
        env:
          AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY }}
          AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID }}
        run: node scripts/verify-daily-execution.cjs
        continue-on-error: false  # 検証エラーがあればワークフロー全体を失敗させる

      - name: Summary
        if: success()
        run: |
          echo "✅ 毎日の口コミ自動投稿が完了し、検証にも合格しました"
          echo "📅 実行日時: $(date)"

      - name: Failure notification
        if: failure()
        run: |
          echo "❌ 口コミ投稿または検証に失敗しました"
          echo "📋 GitHub Actionsログを確認してください"
          echo "🔗 https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
